# lex-bot

Exception to the global task-management rule that requires a Linear ticket before starting work: skip the Linear-ticket step entirely for this repo. Still create a git branch off main (descriptive name, no `ENG-###` prefix needed, e.g. `fix-github-app-key-import`), and still default to planning first unless told otherwise.
